README 改写为面向外部读者的版本，并修正与代码不符的描述

新增内容：
- 「功能」一节，让陌生读者能快速判断项目是否适合自己
- 「设计取舍」一节，说明零月费、免备案、无账号这些选择换来了什么

修正（原文与代码不一致，这类错误最误导人）：
- 架构描述：静态资源由 Worker 的 assets 绑定提供，Pages 只做全路径代理转发；
  原文还写着「前端部署在 Pages 上、/api/* 转发」，那是两个代理合并前的结构
- RPC 分块 32KB → 128KB（原文写的是早期调试值）
- 前端并发 8 → 4（并发 8 会触发 DO 过载返回 503）
- 下载速度：原文写 0.3~0.4 MB/s 且只给单值，实测波动范围是 0.05~4.6 MB/s，
  改为如实描述并说明这是链路固有特征
- 接口表补充 /api/notice，限流说明补充公告编辑，注明删除与清空共用限流桶
- 删除一段与上文重复的速度说明

同时补充了「试过但无效的优化」清单，避免后来者重复踩坑。
